# ValidationChainOfResponsibilityDemo

Chain of responsibility pattern used for validation.

In this demo document entity is validated by a chain of validators.

FluentValidation used. See unit tests first.
